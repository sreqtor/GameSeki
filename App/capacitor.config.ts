import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gamesekiapp',
  appName: 'GameSekiApp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
